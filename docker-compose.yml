version: "3.9"

services:
  web:
    build: .
    ports:
      - "80:8000"
    volumes:
      - ./data:/data          # Il database SQLite persiste qui
    env_file:
      - .env
    restart: unless-stopped
